(function($) {
    console.log('farmer is running');
    $(document).ready(function() {

        var farmerModule = {}
        var dataObject = {}
        var tempUserData = {}

        console.log('farmer is running');

        farmerModule = {

            appState: function() {

                $.get("https://jsonblob.com/api/jsonblob/f4d9dd41-c089-11e6-871b-1946baf47d91", function(data) {
                    tempUserData = data;
                })

                console.log(tempUserData);

            },

            userData: function() {

                $.get("https://jsonblob.com/api/jsonblob/899712de-be27-11e6-871b-1de5240aad90", function(data) {
                    dataObject = data;
                })

                // get single user
                //if no user data exists start with first
                if (tempUserData.length == 0) {
                    console.log('no users exist');
                } else {
                    console.log('user exists');
                }
                // if user data already exists, start with user saved in tempUserData

            },

            urlMaker: function() {

                //url to user contact info
                var baseUrl = 'https://www.facebook.com/geovanio.souza.568/about?section=contact-info';

                window.location.replace(baseUrl);

                this.dataCollector(baseUrl);
            },

            dataCollector: function() {



            }
        }

        farmerModule.appState();

        $('#collect-friend-data').on('click', function() {
            farmerModule.userData();
        })


    });

})(jQuery);